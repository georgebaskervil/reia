{erl_opts, [debug_info]}.

{deps, []}.

% Leex and Yecc are built into Erlang
{project_app_dirs, ["src"]}.

{eunit_opts, [verbose]}.

{plugins, []}.

{shell, [
  {apps, []}
]}.

% Custom rebar3 test provider - run via ./build.sh test instead
